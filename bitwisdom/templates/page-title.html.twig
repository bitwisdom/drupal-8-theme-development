{#
/**
 * @file
 * Theme override for page titles.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 */
#}
{%  
  set id = 'pagetitle'
%}
{% set test = 'testing' %}
{%
  set title_classes = [
    'page-title', 
    'title', 
    'heading'
  ]
%}
{# {% set title = ('Account Name: ' ~ user.account.name) | upper %} #}
{{ kint() }}
{{ title_prefix }}
{% if title %}
  {# Testing: This is a comment. #}
  <h1{{ title_attributes.addClass(title_classes).setAttribute('id', id) }}>{{ title }}</h1>
{% endif %}
{{ title_suffix }}
{{ 444442.35 | round(1) | number_format(1) }}
{{ user.account.roles | sort | reverse | safe_join(', ') }}
<div class="{{ user.account.timezone | clean_class }}" id="{{ user.account.timezone | clean_id }}">Some Text</div>
<a href="http://example.com" title="{{ 'User Website' | t }}">{{ user.account.name }}</a>